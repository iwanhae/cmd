#!/bin/bash
set -euo pipefail

cd ~/work/main

if [[ $# -gt 0 ]]; then
  TARGET="$1"
else
  TARGET=$(git worktree list | fzf | awk '{print $1}' | xargs basename)
fi

git worktree remove --force "../$TARGET"
rm -rvf "../$TARGET"
